import { libGestaoConteudoUtils } from './lib-gestao-conteudo-utils';

describe('libGestaoConteudoUtils', () => {
  it('should work', () => {
    expect(libGestaoConteudoUtils()).toEqual('lib-gestao-conteudo-utils');
  });
});
